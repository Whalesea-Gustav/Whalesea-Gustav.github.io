---
title: 数组
date: 2018-09-18 0:57:00
tags: [JavaScript, Font-end]
categories: 网络编程
---

---
## Array长度  `Array.length`
**对`Array`长度直接赋值会导致`Array`大小变化**
```javascript
var arr = [1, 2, 3];
arr.length; // 3
arr.length = 6;
arr; // arr变为[1, 2, 3, undefined, undefined, undefined]
arr.length = 2;
arr; // arr变为[1, 2]
```


----
## 可以使用索引对array进行修改
如果通过索引赋值，**索引超过范围，同样会引起`Array`大小的变化**
```javascript
var arr = [1, 2, 3];
arr[5] = 'x';
arr; // arr变为[1, 2, 3, undefined, undefined, 'x']
```


---
## indexOf
与`string`类似，`Array`可以通过`indexOf()`来搜索一个指定的元素位置
```javascript
var arr = [10, 20, '30', 'xyz'];
arr.indexOf(10); // 元素10的索引为0
arr.indexOf(20); // 元素20的索引为1
arr.indexOf(30); // 元素30没有找到，返回-1
arr.indexOf('30'); // 元素'30'的索引为2
```


---
## slice
`slice()`对应string的`substring()`，截取`Array`的部分元素，返回一个新的`Array`

**如果不给slice传递参数，则会复制一个array**
```javascript
var arr = ['A', 'B', 'C', 'D', 'E', 'F', 'G'];
var aCopy = arr.slice();
aCopy; // ['A', 'B', 'C', 'D', 'E', 'F', 'G']
aCopy === arr; // false 如果两个引用值同一个对象、数组或函数，则它们是相等。如果指向不同的对象，则它们是不等的。尽管两个对象具有完全一样的属性
```


---
## push 和 pop
`push()`向末位增加若干元素，返回新`Array`的长度
`pop()`把`Array`末尾元素删除掉 并返回这个值 如果是空数组，则返回`undefine`
```javascript
var arr = [1, 2];
arr.push('A', 'B'); // 返回Array新的长度: 4
arr; // [1, 2, 'A', 'B']
arr.pop(); // pop()返回'B'
arr; // [1, 2, 'A']
arr.pop(); arr.pop(); arr.pop(); // 连续pop 3次
arr; // []
arr.pop(); // 空数组继续pop不会报错，而是返回undefined
arr; // []
```


---
## unshift 和 shift
`unshift()`向`Array`头部添加若干元素，返回新`Array`的长度
`shift()`把`Array`的第一个元素删除掉，并将其返回
```javascript
var arr = [1, 2];
arr.unshift('A', 'B'); // 返回Array新的长度: 4
arr; // ['A', 'B', 1, 2]
arr.shift(); // 'A'
arr; // ['B', 1, 2]
arr.shift(); arr.shift(); arr.shift(); // 连续shift 3次
arr; // []
arr.shift(); // 空数组继续shift不会报错，而是返回undefined
arr; // []
```


---
## sort
`sort` 对`Array`进行排序，直接修改`Array`的元素位置


---
## reverse
`Array.reverse();`直接将整个Array元素反转


---
## splice
`splice()`方法是修改Array的**万能方法**，它可以**从指定的索引开始删除若干元素，然后再从该位置添加若干元素**
```javascript
var arr = ['Microsoft', 'Apple', 'Yahoo', 'AOL', 'Excite', 'Oracle'];
// 从索引2开始删除3个元素,然后再添加两个元素:
arr.splice(2, 3, 'Google', 'Facebook'); // 返回删除的元素 ['Yahoo', 'AOL', 'Excite']
arr; // ['Microsoft', 'Apple', 'Google', 'Facebook', 'Oracle']
// 只删除,不添加:
arr.splice(2, 2); // ['Google', 'Facebook']
arr; // ['Microsoft', 'Apple', 'Oracle']
// 只添加,不删除:
arr.splice(2, 0, 'Google', 'Facebook'); // 返回[],因为没有删除任何元素
arr; // ['Microsoft', 'Apple', 'Google', 'Facebook', 'Oracle']
```


---
## concat
`concat()`将当前的`Array`和另外一个`Array`连接起来，返回一个新的`Array`
```javascript
var arr1 = ['A', 'B', 'C'];
var arr2 = arr1.concat([1, 2, 3]);
```
**`concat()`方法并没有修改当前`Array`，而是返回了一个新的`Array`**

`concat()` 可以接受任意个元素或者`Array`，对于`Array`，会将其拆开，然后全部添加到新的`Array`中



---
## join
`join(link-char)`它把当前的Array的每个元素都用指定的字符串连接起来，然后返回连接后的字符串
不改变原本`Array`的内容
```javascript
var arr = ['A', 'B', 'C'];
arr.concat(1, 2, [3, 4]); // ['A', 'B', 'C', 1, 2, 3, 4]
```
